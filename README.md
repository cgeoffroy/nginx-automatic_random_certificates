# nginx-automatic_random_certificates
Custom version of the official Nginx container with an automatic generation of random self-signed certificates. A new set of certificates is created every 'docker run'
